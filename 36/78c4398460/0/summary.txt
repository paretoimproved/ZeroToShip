Everything looks good. Here's a summary of the changes:

**Changes ready to commit:**
- `web/app/explore/page.tsx` — Simplified to use the new `ExploreGrid` component, fixed `EffortLevel` import
- `web/components/explore/ExploreGrid.tsx` — New client component with inline accordion expansion (replaces broken `/idea/[id]` navigation)
- `web/app/debug/[id]/page.tsx` — Deleted (was only for Vercel diagnostics)

**Build:** passes, `/explore` remains a static route (`○`)
**Tests:** 1407 passed, 3 failed (pre-existing email snapshot mismatches, unrelated to these changes)

Want me to commit and push these changes?