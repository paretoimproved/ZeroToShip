/**
 * Usage Limit Middleware for ZeroToShip API
 *
 * Usage limits for on-demand AI generation (fresh briefs, validations)
 * will be reimplemented when on-demand generation is built.
 *
 * Rate limiting (requests/hour) is handled by rateLimit.ts middleware.
 */
